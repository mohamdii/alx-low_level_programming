#include "main.h"
#include <string.h>
/**
 * _strlen - prints the length of a string
 * @s: string input
 *
 * Return: length of the array
 */

int _strlen(char *s)
{
	int y;

	y = strlen(s);
	return (y);
}
