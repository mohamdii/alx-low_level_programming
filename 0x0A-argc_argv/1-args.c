#include <stdio.h>

/**
 * main - main function
 * @argc: arguments number
 *
 * Return: Integer
 */
int main(int argc, char *argv)
{
	int i;

	for (i = 0; i < argc; i++)
	{
		printf
	}
        return (i);
}
