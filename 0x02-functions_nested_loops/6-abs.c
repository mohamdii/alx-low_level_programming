#include <stdio.h>
#include <stdlib.h>
/**
 * _abs - to print absolute value of a variable
 * @x: Checks the number
 *
 * Return: absolute number 
 */

int _abs(int x)
{
	int z;

	z = abs(x);
	return (z);
}
