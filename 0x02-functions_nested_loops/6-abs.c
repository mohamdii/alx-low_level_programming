#include <stdio.h>
#include <stdlib.h>
/**
 * int _abs - to print absolute value of a variable
 * @x: Checks the number
 *
 * @z: Returns the absolute value of input x 
 */

int _abs(int x)
{
	int z;

	z = abs(x);
	return (z);
}
