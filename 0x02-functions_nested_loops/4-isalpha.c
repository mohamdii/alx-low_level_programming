#include <stdio.h>
#include <ctype.h>

/**
 * _isalpha - checks alphabets
 * @c: The number to be checked
 *
 * Return: 1 if its alphabet. 0 otherwise
 */
int _isalpha(int c)
{
	if (_isalpha(c))
	{
		return (1);
	}
	else 
	{
		return (0);
	}
}
