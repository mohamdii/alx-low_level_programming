#include "main.h"

/**
 * _isupper - functions checks upper ulphapets
 * @c: check alphabet
 *
 * Return: 1 if upper. 0 other wise
 */

int _isupper(int c)
{
	if (isupper(c))
		return (1);
	else
		return (0);
}
